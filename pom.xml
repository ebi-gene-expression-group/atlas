<!--
  ~ Copyright 2008-2012 Microarray Informatics Team, EMBL-European Bioinformatics Institute
  ~
  ~ Licensed under the Apache License, Version 2.0 (the "License");
  ~ you may not use this file except in compliance with the License.
  ~ You may obtain a copy of the License at
  ~
  ~ http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
  ~
  ~
  ~ For further details of the Gene Expression Atlas project, including source code,
  ~ downloads and documentation, please see:
  ~
  ~ http://gxa.github.com/gxa
  -->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>uk.ac.ebi.gxa</groupId>
    <artifactId>atlas</artifactId>
    <packaging>pom</packaging>
    <version>0.1.4-SNAPSHOT</version>
    <name>Gene Expression Atlas</name>
    <url>http://www.ebi.ac.uk/gxa/</url>

    <properties>
        <java-version>1.7</java-version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <org.springframework.version>3.2.2.RELEASE</org.springframework.version>
        <json-path.version>0.8.1</json-path.version>
        <test.integration.pattern>**/*IT.java</test.integration.pattern>
        <test.acceptance.pattern>**/acceptance/**/*.java</test.acceptance.pattern>

        <!-- default values for configuration properties,
        they are set to local development machine defaults
        and are overridden in profiles for specific deployment environments-->
        <data.files.location>${user.home}/ATLAS3.TEST/integration-test-data</data.files.location>
        <berkeley.schema.location>/nfs/ma/home/tomcats/ATLAS3.TEST/data/bdb-lime</berkeley.schema.location>
        <h2.database.location>/nfs/ma/home/tomcats/ATLAS3.TEST/data/h2-lime/atlas</h2.database.location>
        <anntation.h2.database.location>/nfs/ma/home/tomcats/ATLAS3.TEST/data/h2-lime/annotations</anntation.h2.database.location>
        <solr.host>ves-ebi-7f</solr.host>
        <gxa.server.url>http://ves-hx-74:8080/gxa</gxa.server.url>
        <deploy.host>
            <!-- this value must be set in environment profiles for whom we want to deploy on tomcat using tomcat manager interface -->
        </deploy.host>
        <deploy.port>
            <!-- this value must be set in environment profiles for whom we want to deploy on tomcat using tomcat manager interface -->
        </deploy.port>
        <integration.test.host/>
        <integration.test.portnumber/>
        <!-- mailtrap.io user: atlas-developers@ebi.ac.uk -->
        <!-- mailtrap.io password: atlas-ebi-14 -->
        <mail.smtp.host>mailtrap.io</mail.smtp.host>
        <mail.smtp.port>2525</mail.smtp.port>
        <mail.smtp.user>feedback-mail-3e3ccf8bbcedeb96</mail.smtp.user>
        <mail.smtp.password>324c3bac45dfb398</mail.smtp.password>
        <mail.smtp.auth>true</mail.smtp.auth>

        <sonar.jdbc.url>
            jdbc:h2:tcp://lime:9092/sonar
        </sonar.jdbc.url>
        <sonar.jdbc.driver>org.h2.Driver</sonar.jdbc.driver>
        <sonar.jdbc.username>sonar</sonar.jdbc.username>
        <sonar.jdbc.password>sonar</sonar.jdbc.password>
        <sonar.host.url>http://lime:9000</sonar.host.url>

        <!-- Code Coverage with SONAR -->
        <sonar.dynamicAnalysis>reuseReports</sonar.dynamicAnalysis>
        <!-- Tells Sonar to reuse existing reports, NEEDED when testing with TestNG! -->

        <sonar.surefire.reportsPath>surefire-reports</sonar.surefire.reportsPath>
        <!-- Tells Sonar where the unit tests execution reports are [ sonar does not like TestNG :( ]-->
        <sonar.surefire.itReportPath>failsafe-reports</sonar.surefire.itReportPath>
        <!-- Tells Sonar where the integration tests execution reports are [ sonar does not like TestNG :( ]-->

        <sonar.java.coveragePlugin>jacoco</sonar.java.coveragePlugin>
        <!-- Tells Sonar that the code coverage tool by unit tests is JaCoCo -->
        <sonar.java.source>1.7</sonar.java.source>

        <sonar.jacoco.reportPath>${project.build.directory}/jacoco.exec</sonar.jacoco.reportPath>
        <!-- Tells Sonar where the code coverage report is -->
        <sonar.jacoco.itReportPath>${project.build.directory}/jacoco-it.exec</sonar.jacoco.itReportPath>
        <!-- Tells Sonar where the code coverage report is -->
        <!-- /Code Coverage with SONAR -->

    </properties>

    <scm>
        <url>scm:git:git@github.com:gxa/atlas.git</url>
        <connection>scm:git:git@github.com:gxa/atlas.git</connection>
        <developerConnection>scm:git:git@github.com:gxa/atlas.git</developerConnection>
        <tag>HEAD</tag>
    </scm>

    <build>

        <finalName>gxa</finalName>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
                <filtering>true</filtering>
            </testResource>
        </testResources>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>0.6.2.201302030002</version>
                    <executions>
                        <execution>
                            <id>prepare-agent</id>
                            <goals>
                                <goal>prepare-agent</goal>
                            </goals>
                            <configuration>
                                <includes>
                                    <include>uk.ac.ebi.atlas.*</include>
                                </includes>
                                <propertyName>jacoco.agent.argLine</propertyName>
                                <!-- used for surefire/failsafe-plugin combination -->
                                <destFile/>
                            </configuration>
                        </execution>
                        <execution>
                            <id>report</id>
                            <phase>prepare-package</phase>
                            <goals>
                                <goal>report</goal>
                            </goals>
                            <configuration>
                                <includes>
                                    <include>uk.ac.ebi.atlas.*</include>
                                </includes>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <plugin>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>2.12.2</version>
                    <executions>
                        <execution>
                            <id>default-test</id>
                            <phase>none</phase>
                            <goals>
                                <goal>test</goal>
                            </goals>
                        </execution>
                    </executions>
                    <configuration>
                        <argLine>${jacoco.agent.argLine},destfile=${sonar.jacoco.reportPath}</argLine>
                        <excludes>
                            <exclude>${test.acceptance.pattern}</exclude>
                        </excludes>
                    </configuration>
                    <dependencies>
                        <dependency>
                            <groupId>org.apache.maven.surefire</groupId>
                            <artifactId>surefire-junit47</artifactId>
                            <version>2.12.2</version>
                        </dependency>
                    </dependencies>
                </plugin>

                <plugin>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>2.12</version>
                    <configuration>
                        <argLine>${jacoco.agent.argLine},destfile=${sonar.jacoco.itReportPath}</argLine>
                        <systemProperties>
                            <property>
                                <name>selenium.test.host</name>
                                <value>${integration.test.host}</value>
                            </property>
                            <property>
                                <name>selenium.test.portnumber</name>
                                <value>${integration.test.portnumber}</value>
                            </property>
                        </systemProperties>
                        <includes>
                            <include>${test.acceptance.pattern}</include>
                            <include>${test.integration.pattern}</include>
                        </includes>
                    </configuration>
                    <executions>
                        <execution>
                            <id>default</id>
                            <phase>none</phase>
                            <goals>
                                <goal>integration-test</goal>
                                <goal>verify</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <plugin>
                    <groupId>org.apache.tomcat.maven</groupId>
                    <artifactId>tomcat7-maven-plugin</artifactId>
                    <version>2.1</version>
                    <configuration>
                        <!-- http port -->
                        <port>9090</port>
                        <!-- optionnal path to a context file -->
                        <contextFile/>
                        <!-- optionnal system propoerties you want to add -->
                        <systemProperties>
                        </systemProperties>
                        <!-- if you want to use test dependencies rather than only runtime -->
                        <useTestClasspath>false</useTestClasspath>
                        <!-- optionnal if you want to add some extra directories in the class loader -->
                        <additionalClasspathDirs>
                            <additionalClasspathDir/>
                        </additionalClasspathDirs>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <version>2.4</version>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.0</version>
                <configuration>
                    <source>${java-version}</source>
                    <target>${java-version}</target>
                    <showDeprecation>true</showDeprecation>
                    <showWarnings>true</showWarnings>
                    <!--<executable>${env.JAVA_HOME_7}/bin/javac</executable>-->
                    <!--<fork>true</fork>-->
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.lazerycode.jmeter</groupId>
                <artifactId>jmeter-maven-plugin</artifactId>
                <version>1.6.0</version>
            </plugin>

        </plugins>

    </build>

    <dependencies>

        <dependency>
            <groupId>javax.inject</groupId>
            <artifactId>javax.inject</artifactId>
            <version>1</version>
        </dependency>

        <dependency>
            <groupId>com.jayway.jsonpath</groupId>
            <artifactId>json-path</artifactId>
            <version>${json-path.version}</version>
        </dependency>

        <!-- Guava -->

        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
            <version>14.0</version>
        </dependency>

        <!--Commons dependencies-->

        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-math</artifactId>
            <version>2.2</version>
        </dependency>

        <dependency>
            <groupId>commons-lang</groupId>
            <artifactId>commons-lang</artifactId>
            <version>2.6</version>
        </dependency>

        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>3.1</version>
        </dependency>

        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-email</artifactId>
            <version>1.2</version>
        </dependency>

        <dependency>
            <groupId>commons-collections</groupId>
            <artifactId>commons-collections</artifactId>
            <version>3.2.1</version>
        </dependency>

        <!--Spring dependencies-->

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-core</artifactId>
            <version>${org.springframework.version}</version>
        </dependency>


        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-expression</artifactId>
            <version>${org.springframework.version}</version>
        </dependency>

        <!--
            Bean Factory and JavaBeans utilities (depends on spring-core)
            Define this if you use Spring Bean APIs (org.springframework.beans.*)
        -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-beans</artifactId>
            <version>${org.springframework.version}</version>
        </dependency>

        <!--
            Application Context (depends on spring-core, spring-expression, spring-aop, spring-beans)
            This is the central artifact for Spring's Dependency Injection Container and is generally always defined
        -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
            <version>${org.springframework.version}</version>
        </dependency>
        <!--
            Required by Spring MVC 3 @Configuration
        -->
        <dependency>
            <groupId>cglib</groupId>
            <artifactId>cglib</artifactId>
            <version>2.2.2</version>
        </dependency>
        <!-- Logging -->

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>1.6.6</version>
        </dependency>

        <dependency>
            <groupId>log4j</groupId>
            <artifactId>log4j</artifactId>
            <version>1.2.17</version>
        </dependency>

        <!-- Test -->

        <dependency>
            <groupId>org.hamcrest</groupId>
            <artifactId>hamcrest-all</artifactId>
            <version>1.3</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>1.9.5</version>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <artifactId>hamcrest-core</artifactId>
                    <groupId>org.hamcrest</groupId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.powermock</groupId>
            <artifactId>powermock-api-mockito</artifactId>
            <version>1.5</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.powermock</groupId>
            <artifactId>powermock-module-junit4</artifactId>
            <version>1.5</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit-dep</artifactId>
            <version>4.11</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>junit-addons</groupId>
            <artifactId>junit-addons</artifactId>
            <version>1.4</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <version>${org.springframework.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-jdbc</artifactId>
            <version>${org.springframework.version}</version>
        </dependency>

    </dependencies>

    <repositories>
        <repository>
            <id>ebi-nexus</id>
            <name>Nexus</name>
            <url>http://www.ebi.ac.uk/intact/maven/nexus/content/groups/public</url>
        </repository>
        <repository>
            <id>ebi-atlas</id>
            <name>Atlas</name>
            <url>http://www.ebi.ac.uk/intact/maven/nexus/content/groups/atlas-repos</url>
        </repository>
        <repository>
            <id>ebi-repo</id>
            <name>The EBI internal repository</name>
            <url>http://www.ebi.ac.uk/intact/maven/nexus/content/repositories/ebi-repo/</url>
        </repository>
        <repository>
            <id>ebi-snapshot-repo</id>
            <name>The EBI internal snapshot repository</name>
            <url>http://www.ebi.ac.uk/intact/maven/nexus/content/repositories/ebi-repo-snapshots/</url>
        </repository>
        <repository>
            <id>oracleReleases</id>
            <name>Oracle Released Java Packages</name>
            <url>http://download.oracle.com/maven</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots/>
        </repository>

        <!--
        * Oracle Drivers: http://www.oracle.com/technetwork/database/enterprise-edition/jdbc-112010-090769.html

        Please download and install those yourself.
        -->
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>apache.snapshots</id>
            <name>Apache Snapshots</name>
            <url>http://repository.apache.org/content/groups/snapshots-group/</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

    <distributionManagement>
        <repository>
            <id>ebi-repo</id>
            <url>http://www.ebi.ac.uk/intact/maven/nexus/content/repositories/ebi-repo</url>
        </repository>
        <snapshotRepository>
            <id>ebi-repo-snapshots</id>
            <url>http://www.ebi.ac.uk/intact/maven/nexus/content/repositories/ebi-repo-snapshots</url>
        </snapshotRepository>
    </distributionManagement>

    <licenses>
        <license>
            <name>Apache Software License 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <!-- Developer Listing -->
    <developers>
        <developer>
            <id>bennyfio</id>
            <name>Benedetto Fiorelli</name>
            <email>bennyfio@ebi.ac.uk</email>
            <organization>EBI</organization>
            <organizationUrl>http://www.ebi.ac.uk</organizationUrl>
            <roles>
                <role>Developer</role>
            </roles>
            <timezone>0</timezone>
            <properties/>
        </developer>
        <developer>
            <id>nsklyar</id>
            <name>Nataliya Kryvych</name>
            <email>nsklyar@ebi.ac.uk</email>
            <organization>EBI</organization>
            <organizationUrl>http://www.ebi.ac.uk</organizationUrl>
            <roles>
                <role>Developer</role>
            </roles>
            <timezone>0</timezone>
            <properties/>
        </developer>
    </developers>

    <modules>
        <module>web</module>
    </modules>

    <profiles>
        <!-- environment profiles -->
        <profile>
            <id>lime</id>
            <properties>
                <deploy.host>lime</deploy.host>
                <deploy.port>14092</deploy.port>
                <data.files.location>/nfs/ma/home/tomcats/ATLAS3.TEST/data</data.files.location>
            </properties>
        </profile>
        <profile>
            <id>www-test</id>
            <properties>
                <data.files.location>/nfs/ma/home/tomcats/ATLAS3.TEST/data</data.files.location>
                <berkeley.schema.location>/nfs/ma/home/tomcats/ATLAS3.TEST/data/bdb</berkeley.schema.location>
                <h2.database.location>/nfs/ma/home/tomcats/ATLAS3.TEST/data/h2/atlas</h2.database.location>
                <anntation.h2.database.location>/nfs/ma/home/tomcats/ATLAS3.TEST/data/h2/annotations</anntation.h2.database.location>
                <mail.smtp.host>smtp.ebi.ac.uk</mail.smtp.host>
                <mail.smtp.port>25</mail.smtp.port>
                <mail.smtp.auth>false</mail.smtp.auth>
            </properties>
        </profile>
        <profile>
            <id>integration.test</id>
            <!-- embedded tomcat started by mvn in bamboo plan -->
            <properties>
                <data.files.location>/nfs/ma/home/tomcats/ATLAS3.TEST/integration-test-data</data.files.location>
            </properties>
        </profile>
        <profile>
            <id>local.integration.test</id>
            <properties>
                <!-- uncomment to integrate with a solr running on localhost rather than the default remote host
                <solr.host>localhost</solr.host>
                -->
                <data.files.location>${user.home}/ATLAS3.TEST/integration-test-data</data.files.location>
                <berkeley.schema.location>${data.files.location}/bdb</berkeley.schema.location>
                <h2.database.location>${user.home}/ATLAS3.TEST/data/h2/atlas</h2.database.location>
                <anntation.h2.database.location>${user.home}/ATLAS3.TEST/data/h2/annotations</anntation.h2.database.location>

            </properties>
        </profile>

        <!-- execution profiles -->
        <profile>
            <!-- deploys war artifact to a remote or local tomcat 7 instance using tomcat manager interface -->
            <id>deploy.tomcat7</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.tomcat.maven</groupId>
                        <artifactId>tomcat7-maven-plugin</artifactId>
                        <configuration>
                            <path>/gxa</path>
                        </configuration>
                        <executions>
                            <execution>
                                <id>tomcat-deploy</id>
                                <goals>
                                    <goal>deploy</goal>
                                </goals>
                                <phase>install</phase>
                                <configuration>
                                    <url>http://${deploy.host}:${deploy.port}/manager/html</url>
                                    <username>tomcat</username>
                                    <password>tomcat</password>
                                    <path>/gxa</path>
                                    <update>true</update>
                                    <!-- optional only if you want to use a preconfigured server.xml file -->
                                    <!--
                                    <serverXml>src/main/tomcatconf/server.xml</serverXml>
                                    -->
                                    <!-- optional values which can be configurable -->
                                    <!--
                                    <attachArtifactClassifier>default value is exec-war but you can customize</attachArtifactClassifier>
                                    <attachArtifactClassifierType>default value is jar</attachArtifactClassifierType>
                                    -->
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

    </profiles>

</project>

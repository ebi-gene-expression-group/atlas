Expression Atlas JavaScript Webpack bundles
===========================================

Development mode
----------------

To build uncompressed bundles into `webapp/resources/js-bundles`:

```
$ npm run dev
```

Production mode
---------------

To build minified production bundles into `webapp/resources/js-bundles`:

```
$ npm run prod
```
/** @jsx React.DOM */

/*global require */
var React = require('react');

var Heatmap = (function (React) {

    return React.createClass({

        render: function () {
            return (
                <div>Hello React!
                </div>
            );
        }
    });

})(React);
